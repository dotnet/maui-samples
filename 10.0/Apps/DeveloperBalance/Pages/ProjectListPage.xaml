<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/maui/global"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="DeveloperBalance.Pages.ProjectListPage"
             x:DataType="ProjectListPageModel"
             x:Name="ProjectList"
             Title="Projects">


    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
                x:Name="AppearingBehavior"
                BindingContext="{Binding Path=BindingContext, Source={x:Reference ProjectList}, x:DataType=ContentPage}"
                EventName="Appearing"                
                Command="{Binding AppearingCommand}" />
    </ContentPage.Behaviors>
    <Grid>
        <CollectionView ItemsSource="{Binding Projects}"
            Margin="{StaticResource LayoutPadding}"
            x:Name="ProjectsCollectionView"
            SelectionMode="Single"
            SelectedItem="{Binding SelectedProject}"
            SelectionChangedCommand="{Binding NavigateToProjectCommand, Source={RelativeSource AncestorType={x:Type ProjectListPageModel}}, x:DataType=ProjectListPageModel}"
            SelectionChangedCommandParameter="{Binding SelectedProject}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="7.5"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="Project">
                    <Border 
                         Background="{AppThemeBinding Light={StaticResource LightSecondaryBackground},Dark={StaticResource DarkSecondaryBackground}}"
                         SemanticProperties.Description="{Binding Name, StringFormat='{0}'}">
                        <VerticalStackLayout Padding="10">
                            <Label Text="{Binding Name}" FontSize="24" />
                            <Label Text="{Binding Description}" />
                        </VerticalStackLayout>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <AddButton
        Command="{Binding AddProjectCommand}" />
    </Grid>
</ContentPage>