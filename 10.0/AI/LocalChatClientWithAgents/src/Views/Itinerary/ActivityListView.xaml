<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="LocalChatClientWithAgents.Views.Itinerary.ActivityListView"
    x:DataType="DayPlanViewModel">

    <VerticalStackLayout Padding="15" BindableLayout.ItemsSource="{Binding Activities}" Spacing="15">
        <BindableLayout.ItemTemplate>
            <DataTemplate x:DataType="Activity">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="36" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" WidthRequest="36" HeightRequest="36" StrokeShape="RoundRectangle 18" StrokeThickness="0"
                            Background="{StaticResource Primary}" VerticalOptions="Start">
                        <Label Text="●" FontSize="16" TextColor="White" HorizontalOptions="Center" VerticalOptions="Center" />
                    </Border>
                    
                    <VerticalStackLayout Grid.Column="1" Spacing="4" VerticalOptions="Start">
                        <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" LineBreakMode="WordWrap" />
                        <Label Text="{Binding Description}" FontSize="14" LineBreakMode="WordWrap"
                               TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                    </VerticalStackLayout>
                </Grid>
            </DataTemplate>
        </BindableLayout.ItemTemplate>
    </VerticalStackLayout>

</ContentView>
