<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:LocalChatClientWithAgents.Models"
    xmlns:views="clr-namespace:LocalChatClientWithAgents.Views.Landmarks"
    x:Class="LocalChatClientWithAgents.Views.Landmarks.LandmarkHorizontalListView">
    
    <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never" Margin="-20,0">
        <HorizontalStackLayout
            x:Name="LandmarksStack"
            BindableLayout.ItemsSource="{Binding Source={RelativeSource AncestorType={x:Type views:LandmarkHorizontalListView}}, Path=Landmarks}"
            Spacing="15"
            Padding="20,0">
            <BindableLayout.ItemTemplate>
                <DataTemplate x:DataType="models:Landmark">
                    <views:LandmarkListItemView
                        Landmark="{Binding .}"
                        LandmarkTapped="OnLandmarkItemTapped" />
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </HorizontalStackLayout>
    </ScrollView>
    
</ContentView>
