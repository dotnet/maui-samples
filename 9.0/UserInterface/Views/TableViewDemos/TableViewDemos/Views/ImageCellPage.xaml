<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:TableViewDemos.Models"
             x:Class="TableViewDemos.ImageCellPage"
             Title="Image cell page">
    <CollectionView ItemsSource="{Binding Sections}"
                   ItemsLayout="VerticalList">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="models:MenuSection">
                <StackLayout Spacing="10" Padding="15">
                    <Label Text="{Binding Title}"
                           FontAttributes="Bold"
                           Margin="0,10,0,5"/>
                    <CollectionView ItemsSource="{Binding Items}"
                                  ItemsLayout="VerticalList">
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:MenuListItem">
                                <Frame Margin="0,5" Padding="10">
                                    <Grid ColumnDefinitions="Auto,*" RowSpacing="5">
                                        <Image Source="{Binding ImageSource}"
                                               HeightRequest="50"
                                               WidthRequest="50"
                                               Aspect="AspectFit"/>
                                        <StackLayout Grid.Column="1" Spacing="5" Margin="10,0,0,0">
                                            <Label Text="{Binding Text}"
                                                   FontAttributes="Bold"/>
                                            <Label Text="{Binding Detail}"
                                                   TextColor="Gray"/>
                                        </StackLayout>
                                    </Grid>
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Command}"/>
                                    </Frame.GestureRecognizers>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>